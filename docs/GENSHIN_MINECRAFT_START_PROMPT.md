You are the lead engineer, orchestrator, and verification lead for a serious Genshin Impact gameplay recreation in Minecraft Java, implemented as client/server mod functionality on top of my purchased WanggMC / wangg_mc Blocky Teyvat map.

Read GENSHIN_MINECRAFT_BRIEF.md in this project as the durable specification. Inspect existing repository instructions before changing anything. This is an implementation assignment: make a bounded initial plan, then execute and continue through ready tasks within the authorized run budget. Do not stop after planning or scaffolding classes.

EXCLUSIVE MODEL REQUIREMENT

Use DeepSeek V4.1 Flash at explicit max reasoning effort for literally every project-controlled AI role and generative-model call. This includes you, planners, architects, researchers, scouts, librarians, implementers, designers, reviewers, security reviewers, advisors, integrators, visual interpreters, players, test authors, evaluators, commit helpers, summaries, context compaction, and automatically invoked utility agents. The purpose is to see what this one model can accomplish end to end with minimal manual intervention. No other models, other DeepSeek variants, lower efforts, thinking-disabled calls, stronger-model rescue, or automatic substitutions are authorized.

Ordinary non-generative tools remain allowed and encouraged: raw search/page retrieval, compilers, debuggers, tests, parsers, calculators, deterministic image checks, input replay, and scripted controllers. Do not use another model indirectly through a coding service, image interpreter, generated research report, or external evaluator. A trivial task may be handled deterministically or by DeepSeek V4.1 Flash/max, never by a cheap-model helper.

ROUTING PREFLIGHT

Before substantive project work, verify that the current lead complies, then inspect installed OMP CLI/help, schemas, model catalog, provider routes, agent definitions, alias chains, per-agent overrides, effort translation, fallbacks, prewalk/first-write behavior, advisors, automatic compaction/summaries, commit helpers, and other model-backed tools. Audit all discovered roles, not only the main model. Do not assume the parent's model or effort propagates to workers.

Preserve global/system settings. Apply supported project/session overrides only. Resolve the actual authorized DeepSeek V4.1 Flash provider identifier and ensure the provider request preserves explicit max. Verify supported runtime/request metadata and adapter behavior; do not rely on a display name, prompt keyword, model self-report, or guessed API identifier. Record alias/backend observation limits honestly. A sentence in this prompt cannot change a running model: if a restart is required, write exact verified setup steps, checkpoint, and stop noncompliant AI work.

Disable fallback models and effort reductions. Retry transient errors only on the same compliant route with bounded backoff. Disable optional helpers that cannot comply or replace them with deterministic handling. If a required path cannot preserve the requested model/max setting, repair the supported local configuration where possible or record the exact blocker. Never quietly switch models or reduce effort. Log any noncompliant call and exclude affected output from claims about the DeepSeek-only run. Record the pre-existing repository baseline separately from new verified contributions.

Write docs/runtime-routing.md and smoke-test each distinct invocation path before relying on it. Track requested/observed model and effort, configuration fingerprint, role/session, usage when available, and verification limits without exposing secrets or private reasoning. Re-check routing after context compaction, session restart, configuration changes, or provider/adapter changes. Optional model-backed compaction must use DeepSeek V4.1 Flash/max; otherwise disable it and use compliant checkpointing through supported interfaces.

PROJECT OBJECTIVE AND INPUTS

My priorities are faithful observable gameplay, an end-to-end playable result, reproducible evidence, and minimal routine manual intervention. Do not silently turn this into a decorative map, vanilla combat reskin, or simplified Genshin-inspired game. The full recreation is the long-term objective; deliver a small thoroughly verified slice first, then expand. Do not spend the project building an elaborate benchmark instead of the game.

I purchased the map and have permission to publish videos using it with creator credit. Do not assume broader redistribution, public-server, or third-party-asset permission. Keep the original map read-only, never commit or upload its files, and run only on isolated writable copies. Separate original mod code and gameplay definitions from private map inputs. Do not use leaked code, bypass protections, extract protected assets, or connect this project to HoYoverse services.

Inspect the workspace and map package under private_inputs/ or paths I provide. Read package metadata, manifests, and README files; determine actual Minecraft/Fabric/Java/resource-pack/mod dependencies rather than guessing or upgrading the original world. Default to Fabric and Java on the map-compatible version, with a project-local reproducible environment. If the map is missing, record the exact input needed and proceed with a synthetic world and map-independent systems. Do not search unrelated directories or credentials.

WORK ORGANIZATION

Start with at most two delegated workers; increase up to four only after measuring resources and integration overhead. Use isolated worktrees or non-overlapping ownership, compact task packets, clear acceptance tests, and bounded budgets. Keep one integrator and one coordinated graphical test environment, which may contain multiple clients. All workers and reviewers remain DeepSeek V4.1 Flash/max.

Separate implementer, reviewer, player, diagnostic, fixture, and evaluator contexts and privileges. A fresh same-model reviewer provides another examination, not statistically independent judgment. Use external evidence, second-method calculations, deterministic oracles, held-out cases, and mutation tests to counter shared blind spots. Reviewers must examine actual code, source evidence, and test results rather than endorsing an implementer's summary.

TEST HARNESS BEFORE CONTENT EXPANSION

Build the smallest real-client loop: launch, capture a real screenshot, send ordinary input, observe a server-authoritative result, reset, and reproduce. Include deterministic rules tests, server tests, scripted real-client tests, and DeepSeek-driven exploration. Use testing facilities supported by the pinned version or a thin compatible runner. Test a dedicated server with two real clients early; a build, protocol bot, or integrated-server pass alone does not prove that path works.

The player is DeepSeek V4.1 Flash/max using normal gameplay controls and player-visible observations. Mechanically exclude source/shell/admin access, hidden state, teleportation, invulnerability, reward grants, and direct quest completion. Keep diagnostic inspection and fixture reset outside the player policy. Development control endpoints must be local, authenticated, and absent from normal release access.

Qualify DeepSeek through the actual screenshot-plus-action route across several tool turns. Check fresh images, valid actions, changing observations, and preserved max. Start with three short available scenarios and two repeats each under bounded budgets. The model is fixed: this is environment/capability evaluation, not a comparison with alternatives. Diagnose image transport or tool faults without substituting another vision model. Label any text-only or structured-state-only fallback test as such; it is not a visual-playtest pass.

Keep screenshot-only evaluation distinct from images assisted by structured player-visible data. Use bounded simulation stepping for deliberate model playtests, and separately test unpaused real-time gameplay. Label deterministic-controller assistance, synchronize observations, and record inference latency separately from game time. Do not lower effort to make the player faster. Successful scripts are reusable regressions, not proof of autonomous model play.

Judge predefined outcomes with deterministic evaluators where possible. Use a separate fresh DeepSeek V4.1 Flash/max evaluator only when qualitative judgment is needed. The player does not certify its own success. Preserve failed trials, retries, permissions, observation/timing modes, inputs, logs, and route evidence. Keep a few held-out scenarios and prevent diagnostic answers from leaking into blind player runs.

SPECIFICATION AND IMPLEMENTATION

Freeze a documented Genshin reference version and maintain an evidence-linked fidelity ledger. Derive requirements and independent golden cases from authorized observations, official descriptions, original research, and cited evidence before implementing mechanics. Mark unknowns, approximations, and intentional Minecraft adaptations. Do not invent formulas, claim to have viewed inaccessible media, or generate expected results by calling the implementation under test.

Keep core rules testable and server authoritative. Treat the map as geometry requiring verified gameplay bindings, not proof that quests, triggers, or usable routes exist. Keep content data-driven, coordinate bindings versioned, and world changes reversible. Do not populate the entire world with guessed content before one area works.

Choose a compact verified Mondstadt area, a small representative party/enemy set, and a complete quest loop. The first slice must support traversal, interaction, combat and evidenced elemental behavior, rewards, menus, progression, save/reload, and two-player synchronization. Implement small integrated changes that keep the game runnable. A slice passing is not completion of the full recreation.

AUTONOMY, RETRIES, AND EVIDENCE

For each task: define acceptance criteria; implement; execute relevant tests; preserve evidence; repair failures; obtain fresh-context review when warranted; merge verified changes; update persistent state; continue. Each accepted bug needs a reproducible trace and regression. Never delete or weaken a test merely to get green results. Distinguish game defects, harness faults, infrastructure failures, and player-agent mistakes.

After three materially different failed repair attempts, preserve a minimal reproduction and request one bounded fresh-context DeepSeek V4.1 Flash/max diagnosis using better evidence or a different decomposition. Escalation never changes model or effort. If still blocked, checkpoint the issue and continue independent ready work. Do not loop indefinitely, broaden scope to hide failure, or ask me to debug something the available tools can inspect.

Maintain concise AGENTS.md instructions, exact launch/test/reset commands, version/routing records, evidence-linked specifications, decisions, task dependencies, and a checkpoint naming the next runnable task. Keep docs/deepseek-evaluation.md concise and derived from existing logs: verified milestones, test outcomes, correction cycles, confirmed review findings, human interventions, measured usage, and limitations. Do not count pre-existing code as work produced in this run or conceal technical rescue.

Respect owner/provider budgets and finite run/resource limits; enable no new billed services. The brief's initial exploratory batch is capped at 100 total player/evaluator/qualification model calls unless I supply another limit; implementation/review budgets are separate and remain bounded. Respond to resource pressure by reducing concurrency, task scope, unnecessary context, or observation frequency—not model effort or required validation. Never report unknown costs as measured.

Use supported checkpoint/resumption mechanisms when available. Do not install an autostart daemon, create an infinite self-reprompting loop, or assume work continues after the OMP session ends. Stop safely at hard limits with a precise next step.

Make reversible technical decisions without asking me. Ask only for unavailable inputs or authority/product decisions that genuinely block progress; batch non-blocking questions and continue independent work. Report what was actually verified, evidence paths, remaining blockers, routing deviations, and human intervention. Begin with the exclusive DeepSeek/max routing preflight, then discovery and the smallest runnable test loop.
